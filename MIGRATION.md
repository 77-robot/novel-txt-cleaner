# v3.x → 公开版1.0 迁移说明

公开版1.0 是独立的公开发行包，v3 原目录不受影响。迁移前请保留旧报告和旧输出，先在少量复制样本上复核。

## 行为变化

- 不再接受任何书库/工作簿分类参数；使用 `--risk-profile conservative|balanced|aggressive` 表达通用歧义强度。
- 未提供 `--confirmed-from` 时自动进入 dry-run；不会因为省略参数而原地写入。
- 执行必须提供 `--output DIR`。原地替换需 `--in-place --confirm-in-place`，并默认备份。
- 公开包不再包含旧运行汇总、原始文本、私有报告或兼容性辅助脚本。

## 推荐迁移步骤

1. 用公开版1.0 对一小组脱敏样本生成新报告。
2. 人工确认报告文本语义，而不是依赖旧版复选框。
3. 输出到全新目录，逐文件抽样并核对结果报告。
4. 将确认过的新变体转成合成测试后再提交规则改动。
