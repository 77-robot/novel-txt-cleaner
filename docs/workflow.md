# 安全工作流

```text
输入 TXT → dry-run 报告 → 人工裁决 → confirmed-from 执行 → 输出目录 → 信号/二进制抽查
```

报告中的空白裁决沿用机器建议；“保留/不删”覆盖机器建议；“删除：X/改删：X”只授权指定片段。报告解析失败或人工意见冲突时应停止执行，修正报告后重试。

执行输出应与输入目录树一一对应。生产环境建议保留原目录只读，并把报告、输出和备份放在单独目录。
