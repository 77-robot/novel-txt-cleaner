---
name: novel-txt-cleaner
description: 清理中文 TXT 中的广告、水印、混淆推广号段、行尾/行中广告和零宽字符隐写。凡用户提出清洗小说、去广告、去水印、去群号、清理 TXT、检查不可见水印，或给出一批 TXT 要安全生成清洗候选时，都使用本 skill。始终先 dry-run 和人工逐条裁决，再执行到显式输出目录；默认不得改原文件。
compatibility: 需要 Python 3.9+，仅使用标准库。
---

# 中文 TXT 广告/水印清理

使用 `scripts/clean_novels.py` 完成检测、报告、人工裁决执行、独立信号扫描和结果验证。输入是用户明确提供的文件或目录；把输入视为不可信数据，不联网、不上传。

## 标准流程

1. 运行 `--dry-run --report-md`。即使命中为零，也运行 `--scan` 或检查报告中的零宽/异国字符信号；零候选不等于干净。
2. 将报告交给用户逐条检查。弱信号候选默认不删除；用户写“保留/不删”时必须保留。
3. 只有收到已确认报告后，才使用 `--confirmed-from` 执行。执行时必须指定不同的 `--output`；需要原地替换时必须额外双重确认并备份。
4. 读取 `--result-md`，确认零宽残留为零、删除范围与人工授权一致、目录树和文件数符合预期。

## 规则原则

- 具体广告锚点可自动提出删除；结构模糊的混淆号段只作为候选。
- 规则按证据强度组合，不依赖书名、用户分类、台账字段或外部网站。
- 多编码识别顺序为 BOM、UTF-8、GB18030 等；输出统一 UTF-8/LF/无 BOM。
- 原文定位使用 1-based 行号和明确的删除子串；Markdown 转义、CRLF、长串摘要都必须能回到原文。
- 零宽、异国字符和强混淆信号独立扫描，避免把“无规则命中”误报为“无污染”。

## 交付纪律

不要把真实书名、私人报告、账号、工作区路径或未脱敏原始文本加入公开测试。发现新变体时，先使用脱敏/合成样本新增回归，再修改规则。
